import 'package:flutter/material.dart';

class AccountLevelPage extends StatelessWidget {
  const AccountLevelPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('مستوى الحساب')),
      body: const Center(child: Text('صفحة مستوى الحساب')),
    );
  }
}
