import 'package:flutter/material.dart';

class AdminMarqueePage extends StatelessWidget {
  const AdminMarqueePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('شريط الإعلانات')),
      body: const Center(child: Text('صفحة شريط الإعلانات (قيد التطوير)')),
    );
  }
}
